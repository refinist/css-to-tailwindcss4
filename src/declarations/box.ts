// Placeholder for box-model declarations that don't fit elsewhere.
// Kept separate so additions don't bloat layout.ts.

import type { HandlerTable } from './dispatch.ts';

export const boxHandlers: HandlerTable = {};
